<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TiendaVirtual" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/celeste_claro</item>
    </style>

    <style name="Theme.TiendaVirtual" parent="Base.Theme.TiendaVirtual" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Customize your light theme here. -->
        <item name="android:windowSplashScreenBackground">@color/color_splash</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/compras</item>
        <item name="android:windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.TiendaVirtual</item>
    </style>
</resources>